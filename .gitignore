*.o
*.a
*.so
*.d

.cache
.claude
.omc
.gjc

__pycache__/

build
results
docs

*.md

.DS_Store
.agent
.codex
docs/

# Calibration measurements are a tracked deliverable: the noise bounds baked
# into src/util/params.cpp are derived from them and must stay auditable.
# Git will not descend into an excluded directory, so the parent has to be
# re-included before the exception can apply.
!scripts/results/
scripts/results/*
!scripts/results/calibration/
